"""Game implementations.

Each game lives in its own subpackage (see ``minecraft/``) with a server
module and a workload package. Games and workloads are selected by name in a
configuration file; the name-to-class registries are in
:mod:`yardstick_benchmark.config`.
"""
